// import axios from "axios";

// export function useAxios() {
//   const userInfo = useUserInfo();
//   return axios.create({
//     baseURL: "http://127.0.0.1:8000",
//     headers: { common: { Authorization: `Bearer ${userInfo.content.token}` } },
//   });
// }
